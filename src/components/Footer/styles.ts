import styled from "styled-components";

export const Container = styled.footer`
  background: var(--purple);
  padding: 0.25rem;
  width: 100%;

  p {
    padding: 0.5rem;
    text-align: center;
    color: var(--black);
  }
`;
